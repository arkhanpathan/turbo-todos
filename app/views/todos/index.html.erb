<div class="" style="float:right">
  <%= link_to t('navigation.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "bg-red-50 px-3 py-2 rounded inline-flex items-center justify-center text-red-600" %>
</div>
<div class="mx-auto max-w-3xl mt-24">
  <div class="mb-6">
    <h1 class="font-bold text-4xl">Turbo Todos</h1>
  </div>

  <%= render "form", todo: Todo.new %>

  <div id="todos" class="divide-y list-none">
    <%= render @todos %>
  </div>
</div>
